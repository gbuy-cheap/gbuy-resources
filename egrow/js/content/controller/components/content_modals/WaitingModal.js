/**
 * It represents the waiting modal which is displayed whenever the products
 * are loading in the background (being requested).
 */
class WaitingModal extends ContentModal {
    constructor () {
        super("#waitingModal");
    }
}
